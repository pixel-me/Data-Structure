#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        long sum=0;
        cin >> n;
        int n, m, i, j;
        cin >> n >> m;
        int a[n][m];
        for (i = 0; i < n; i++)
        {
            for (j = 0; j < m; j++)
            {
                cin >>a[i][j];
            }
        }
        int k;
        cin>>k;
        for (i = 0; i < n; i++)
        {
            for (j = i; j < m; j++)
            {
                
            }
            
        }
        
    }
    return 0;
}